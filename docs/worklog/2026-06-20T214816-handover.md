# 引継ぎドキュメント (Handover Record) - Phase 0 完了 & Phase 1 実装への移行

* **作成日時**: 2026-06-20T21:48:16+09:00
* **作成者/引継ぎ元**: Human & Antigravity (Antigravity 3.5 Flash)
* **引継ぎ先**: 次のセッションエージェント (Next Session Agent) / ChatGPT / Human

---

## 1. 🎯 現在の目的 (Current Goal)

Kanon の基礎となる **Phase 0: Knowledge Foundation (知識と記憶の土台)** の設計および配置がすべて完了。
これ以降は、設計・ADR構築の段階から、能力・技術の **Phase 1 (Chat/Survival & Observation) の Exit Criteria に則った実コードの実装フェーズ**へと移行する。

---

## 2. 🚦 現在の状況 (Current State)

* **Repository Layout**:
  [README.md](file:///Users/nabe/src/github.com/nabe126/kanon/README.md) に規定された「脳（Core）」「記憶（Memory）」「仕事/連携（Workhub）」の論理境界に沿って物理的配置と `.gitignore` の機密隔離設定が完了。
* **Agent Constitution**:
  エージェントの行動基準となる絶対規範 [.agents/AGENTS.md](file:///Users/nabe/src/github.com/nabe126/kanon/.agents/AGENTS.md) が確定。
* **Memory Layout**:
  `memory/` 配下に Git 追跡された6つの記憶ディレクトリが成立。
* **Roadmap**:
  価値提供を表す「能力ロードマップ」とインフラを示す「技術ロードマップ」が分離され、それぞれ完了判定基準（Exit Criteria）が明文化された。
* **ADR-008の修正**:
  将来的なリポジトリ分割（Monorepo ➔ B. 2分割 ➔ C. 3分割）の検討において、`workhub` を単なる UI ではなく「仕事・タスク執行・他システム連携を司るビジネス・調整領域」と再定義し承認済。

---

## 3. 🔍 未解決の課題・懸念事項 (Open Issues)

* 現段階でのリポジトリ物理分離は不要（Monorepo維持）だが、コード実装にあたり `Core` ロジックと `Twin` ロジックが密結合にならないよう、フォルダ・クラス設計で疎結合を保証する必要がある。
* エージェントコンテナは非特権で実行されるため、Controller（特権）に再起動やバックアップ指示を投げるための IPC / API 接続の通信プロトコル設計が必要。

---

## 🚀 次に推奨されるアクション (Next Recommended Actions)

設計資料の作成は完了したため、**技術 Phase 1 (Survival & Observation) の実装**を開始してください。

1. **自動適用前検証スクリプトの作成**:
   * エージェントがコードを適用する前に、別プロセスで `python -m py_compile` 等による構文チェックおよび `import check` を実行する検証エンジンを `system/` または `ai-agent/workspace/src/` 配下に実装する。
2. **Controller 側の自動ロールバック監視スクリプトの実装**:
   * [controller/](file:///Users/nabe/src/github.com/nabe126/kanon/controller/) ディレクトリ配下に、エージェントコンテナの生存やヘルスチェックポート（5000）を監視し、タイムアウト時に `backups/` から LKG バージョンを自動で書き戻して再起動するスクリプト（Python または Bash）を実装する。
3. **エージェント側 `view_logs` ツールの実装**:
   * エージェントが自身のエラーを読み取って自律修復できるよう、コンテナログ（`docker logs`）を取得するツールを作成・追加する。

---

## ⚠️ リスクと制約事項 (Risks)

* 実装を行う際も [.agents/AGENTS.md](file:///Users/nabe/src/github.com/nabe126/kanon/.agents/AGENTS.md) の **Never Rules** を厳格に遵守すること。
* コンテナ環境変数の注入設定などを壊さないよう、Docker構成の変更は最小限に留めること。
